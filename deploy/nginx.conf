# Nginx 配置 - Streamlit 反向代理
# 部署路径: /etc/nginx/sites-available/investment-app
# 启用方式: ln -s /etc/nginx/sites-available/investment-app /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name oneweblog.cn;

    # Streamlit 反向代理
    location /app/ {
        proxy_pass http://127.0.0.1:8501/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}

# HTTPS 配置（运行 certbot 后自动生成）
# sudo certbot --nginx -d oneweblog.cn
