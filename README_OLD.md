# 用户画像-投资策略匹配系统

基于行为特征与 PCA 降维的量化策略推荐引擎，通过 **径向惩罚余弦相似度** 为用户匹配风格一致的投资策略。

## 背景

在量化投资领域，"什么样的策略推荐给什么样的客户"是一个经典问题。本项目提供一套**传统统计学方法**的解决方案：从交易流水中提取 12 维行为/偏好/风险代理特征，经 PCA 降维后在特征空间中度量用户与策略的匹配度。

> 本项目的统计学路线与团队并行的 **Word2Vec 深度学习路线**形成交叉验证关系。

## 核心假设

整个匹配体系建立在四个基本假设之上：

1. **收益是过滤条件，不是匹配维度** — 没有客户会追求低收益。策略收益率只需满足"不低于用户历史收益"的门槛，不参与相似度计算。
2. **风格一致带来信任** — 用户更愿意接受"看起来像我自己会选的策略"。持仓周期、换手率、集中度等行为特征的对齐比收益率相似更重要。
3. **行为画像优于绝对收益** — 当缺乏期初资金数据时，从交易模式中反推用户"喜欢做什么"比强行估算绝对收益率更稳健。
4. **用户与策略可在同一特征空间中度量** — 无论是策略还是用户，"持仓周期""换手率""买卖对称性"等概念的定义是普适的。

## 方法概要

### 三层特征体系（12 维）

| 层次 | 维度 | 特征 | 刻画什么 |
|------|------|------|---------|
| **交易行为** | 6 | 持仓周期、换手率、买卖对称性、持仓集中度(HHI)、处置效应、胜率 | 用户"怎么做交易" |
| **资产偏好** | 3 | ETF占比、价格区间偏好、分仓均匀度 | 用户"喜欢买什么" |
| **风险代理** | 3 | 亏损幅度、波动偏好、趋势偏好 | 用户"能承受多大风险" |

所有 12 维特征**仅从交易流水计算**，无需行情数据或账户资金信息。

### 相似度度量：径向惩罚余弦

$$\text{sim}(u, s) = \frac{u \cdot s}{\|u\| \cdot \|s\|} \times \exp\left(-\lambda \cdot \left|\log\frac{\|u\|}{\|s\|}\right|\right)$$

同时捕捉**方向一致度**（余弦）与**模长差异**（径向惩罚），兼顾两种信息：
- 方向一致 + 模长接近 → 满分
- 方向一致 + 模长悬殊 → 降权
- 方向相反 → 负值（保留"风格相反"信号）

### 超参数

| 参数 | 默认值 | 作用 |
|------|--------|------|
| **β** (beta) | 0.5 | 行为特征 vs 非行为特征的权重比 |
| **λ** (lambda) | 1.0 | 径向惩罚的强度 |

## 快速开始

### 环境

```bash
pip install numpy pandas scikit-learn scipy matplotlib seaborn openpyxl
```

### 运行完整流水线

```bash
python pipeline.py
```

执行后将在 `output/` 目录下生成：

| 文件 | 内容 |
|------|------|
| `strategy_features_v2.csv` | 7 个策略的 12 维特征向量 |
| `user_features_v2.csv` | 3 个模拟用户的 12 维特征向量 |
| `pca_results_v2.csv` | 所有实体在 PCA 空间的投影坐标 |
| `pca_info_v2.json` | PCA 模型详情（载荷、解释方差、β、λ） |
| `recommendations_v2.json` | Top-3 推荐 + 维度级归因 + 弹窗话术 |
| `strategy_nav.png` | 策略净值曲线图 |
| `pca_scatter_v2.png` | PCA 散点图（策略 vs 用户） |
| `similarity_heatmap_v2.png` | 三种度量的相似度热力图 |
| `pca_loadings_v2.png` | PCA 特征载荷图 |
| `feature_radar_v2.png` | 用户特征雷达图对比 |

### 生成模拟数据

用于大规模测试和 Word2Vec 训练：

```bash
python generate_simulated_data.py
```

生成 500 个模拟策略 + 200 个模拟用户的交易数据，输出至 `output/simulated_data/`。

## 数据

### 策略数据（真实）

7 个量化策略，约 3 年历史数据，每个策略包含：
- `daily_value.csv` — 日度净值曲线
- `trades.csv` — 交易明细
- `funds.csv` — 资金流水

存放于 `净值_交易_资金及字段说明（相关性数据分析）/products_export_20260518_163122/` 下按策略名分目录。

### 用户数据（模拟）

3 个模拟用户的交易流水：
- 用户 A：127 笔交易，长线低频风格
- 用户 B：1727 笔交易，超短线高频风格
- 用户 C：285 笔交易，短线中频风格

## 匹配结果示例

| 用户 | 画像 | 推荐策略 | 匹配度 |
|------|------|---------|--------|
| A | 长线低频，持仓 52 天 | 宽指配置_V251216 | 13.4% |
| B | 超短线高频，持仓 2 天 | HX_朝花夕拾 | 17.8% |
| C | 短线中频，胜率 67% | HX_朝花夕拾 | 22.3% |

每个推荐都附带**维度级归因**，说明哪些特征最相似、哪些最不同。

## 流水线架构

```
数据加载 → 策略特征提取(12维) → 用户特征提取(12维)
  → β 加权 → PCA 降维(90%方差) → 径向惩罚余弦计算
  → Top-3 推荐 → 可解释归因 → 输出
```

关键实现细节：
- **FIFO 配对**：买卖逐笔匹配计算已实现收益和持仓周期
- **PCA 保留模长**：标准化拟合方向，去均值但不缩放投影，保留"极端程度"信息
- **ETF 识别**：代码前缀模式匹配（15/51/56/58/12 开头）

## 局限性

1. 用户数据量有限（仅 3 个模拟用户）
2. β 超参数未通过问卷校准
3. 行业维度使用 ETF 占比代理，缺少申万行业精确映射
4. 无法精确计算用户持仓市值（无个股行情数据）

详见 `report.md` 第十节。

## 改进方向

- [ ] 接入风险测评问卷数据校准 β
- [ ] 滚动窗口匹配（最近 N 天）
- [ ] 28 维申万行业分布 + JS 散度
- [ ] 与 Word2Vec 路线做一致性分析
- [ ] 基于真实用户数据的 A/B 测试

## 项目结构

```
├── pipeline.py                  # 主流水线代码
├── generate_simulated_data.py   # 模拟数据生成器
├── report.md                    # 详细数据分析报告
├── README.md                    # 本文件
├── output/                      # 输出结果
│   ├── recommendations_v2.json
│   ├── pca_results_v2.csv
│   └── ...
├── 净值_交易_资金及字段说明（相关性数据分析）/
│   └── products_export_20260518_163122/  # 策略数据
└── 模拟账户A/B/C的记录.xlsx      # 用户交易流水
```

## 相关文档

- [`report.md`](report.md) — 完整数据分析报告，含假设体系、公式推导、结果解读
- 团队 Word2Vec 路线 — 机器学习交叉验证（进行中）

## License

内部项目，仅供内部使用。
