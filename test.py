text = """本项目面向量化投顾场景，构建了一套基于交易行为的个性化策略推荐引擎。用户仅需填写问卷，（无需账户资金信息与上传交易流水，但支持用户上传），系统即可自动刻画投资画像、动态更新画像（与交易信息的更新同步）并推荐风格一致的策略。                                                                            
核心工作流：注册登录 → 三级问卷初始化画像 → 上传交易数据 → EMA 动量式画像更新 → 三后端并行匹配（统计 PCA + LSTM 序列 + 融合加权） → 可解释归因 + 弹窗话术生成。
四大亮点：
1. 独创三层 12 维特征体系——从持仓周期、换手率、处置效应等行为特征到隐含风险偏好，零外部依赖、仅凭交易流水刻画完整画像。
2. 径向惩罚余弦相似度——同时捕捉方向一致性与极端程度差异，兼顾"风格像不像"和"力度够不够"。
3. 两阶段混合架构——统计路线保证可解释，深度学习路线（BiLSTM + Word2Vec）捕捉序列语义，融合加权实现精度与可信度的平衡。
4. 网页应用实现代码仓库——现阶段设计了网页应用，能够直观展示项目完整工作流程，支持用户的注册与登录。同时团队有完善的代码版本管理，方便跟进工作。"""


def word_count(text):
    lines = text.splitlines()
    word_count = 0
    for line in lines:
        line = line.strip()
        if len(line) > 0:
            word_count += len(line.split())
    return word_count


if __name__ == '__main__':
    print(word_count(text))